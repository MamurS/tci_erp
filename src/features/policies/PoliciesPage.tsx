import { StubPage } from '../../components/ui/StubPage'

/** Placeholder — real UI will be specified in a later phase (see CLAUDE.md roadmap). */
export function PoliciesPage() {
  return <StubPage titleKey="nav.policies" />
}
